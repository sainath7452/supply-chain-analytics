# Power BI

- `SupplyChain_Analytics.pbix` — the report (add your saved file here).
  If it exceeds GitHub's 100 MB limit, use Git LFS: `git lfs track "*.pbix"`.
- `screenshots/` — page exports.

Connects to Snowflake `SUPPLY_CHAIN.MART` (Import mode) via the `BI_READER_ROLE`
role on the `WH_REPORT` warehouse.
